version: "3.7"
services:
  mutant-app:
    build: .
    links:
      - database
    ports:
      - target: "${TARGET_PORT}"
        published: "${PUBLISH_PORT}"
        protocol: tcp
        mode: ingress
    #env_file:
    #  - ${ENV_FILE}
    #volumes:
    #  - logs:/home/novotrans/logs
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: '${CPU}'
          memory: ${RAM}
  database:
    image: mongo:4.4.0


    #secrets:
    #  - DB_USER_SGC
    #  - DB_PASS_SGC
#volumes:
#  logs:
#    external: true

#secrets:
#  DB_USER_SGC:
#    external: true
#  DB_PASS_SGC:
#    external: true

